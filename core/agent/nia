#!/usr/bin/env python3
"""
NIA CLI launcher.

This wrapper should behave like the installed `nia` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from nia_cli.main import main
    main()
